Config = {
    ChangeResMsg = "You will get kicked in %s seconds, change your resoultion to 16:9", -- message every MsgTimer seconds to give before kicking the user
    KickMsg = "You have been kicked, change your resolution to 16:9", -- message to show when user gets kicked
    Timer = 30, -- timer to give the time to change resolution
    MsgTimer = 2000, -- show message every two seconds to not spam 
    CheckTimer = 1000 -- check for resolution every 1 second you can change it to 2 seoconds 
}